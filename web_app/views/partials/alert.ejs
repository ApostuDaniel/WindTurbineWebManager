<div class="alert" id="id<%= alert.id %>">
    [ <%= alert.timeStamp %> ] <%= alert.user.firstName %> <%= alert.user.lastName %>, there seems to be a
    problem with your turbine <%=alert.turbine.name %>
  <div>
    <button onclick="redirectToDetailsPage(`<%= alert.turbine._id %>`, `<%= alert.id %>`)">Check it out</button>
  </div>
</div>
